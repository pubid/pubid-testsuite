---
- id: gb.debt.1388947605755474777
  input: "# Social-group standard (T/{ORG}) — Chinese typography uses em-dash for
    year"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse Chinese Standard
    identifier ''# Social-group standard (T/{ORG}) — Chinese typography uses em-dash
    for year'': Failed to match sequence (PUBLISHER_CODE SPACE NUMBER PART? YEAR ALL_PARTS_F'
- id: gb.debt.1917493891070848423
  input: "# separator; the parser accepts both forms and normalizes to ASCII dash."
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse Chinese Standard
    identifier ''# separator; the parser accepts both forms and normalizes to ASCII
    dash.'': Failed to match sequence (PUBLISHER_CODE SPACE NUMBER PART? YEAR ALL_PARTS_FLAG?'
