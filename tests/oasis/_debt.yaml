---
- id: oasis.debt.4519095904824870169
  input: "# Curated sample of real OASIS identifiers from relaton-data-oasis (~605
    records)."
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# Curated sample of real OASIS identifiers from relaton-data-oasis (~605 records).'':
    Failed to match sequence (PREFIX SLUG) at line 1 char 1.'
- id: oasis.debt.2479317875387018879
  input: "# Every line must satisfy parse(x).to_s == x (100%) — the verbatim `original`"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# Every line must satisfy parse(x).to_s == x (100%) — the verbatim `original`'':
    Failed to match sequence (PREFIX SLUG) at line 1 char 1.'
- id: oasis.debt.1627181850458680952
  input: "# contract guarantees this regardless of how much decomposition succeeds."
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# contract guarantees this regardless of how much decomposition succeeds.'':
    Failed to match sequence (PREFIX SLUG) at line 1 char 1.'
- id: oasis.debt.84457451093514991
  input: "# Covers: bare spec, version forms (v/bare/3-part/glued), the three part"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# Covers: bare spec, version forms (v/bare/3-part/glued), the three part'':
    Failed to match sequence (PREFIX SLUG) at line 1 char 1.'
- id: oasis.debt.736910407767189012
  input: "# spellings, all stage tokens, labels, both fragment orders, and the handful
    of"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# spellings, all stage tokens, labels, both fragment orders, and the handful
    of'': Failed to match sequence (PREFIX SLUG) at line 1 char 1.'
- id: oasis.debt.2185353085096881817
  input: "# malformed records (embedded space / stray `]`)."
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# malformed records (embedded space / stray `]`).'': Failed to match sequence
    (PREFIX SLUG) at line 1 char 1.'
- id: oasis.debt.4519383841424777751
  input: "# --- bare spec name only ---"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# --- bare spec name only ---'': Failed to match sequence (PREFIX SLUG) at line
    1 char 1.'
- id: oasis.debt.1424960331401519356
  input: "# --- spec + version ---"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# --- spec + version ---'': Failed to match sequence (PREFIX SLUG) at line 1
    char 1.'
- id: oasis.debt.107326680398769887
  input: "# --- bare (no v) version ---"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# --- bare (no v) version ---'': Failed to match sequence (PREFIX SLUG) at line
    1 char 1.'
- id: oasis.debt.388044405249692667
  input: "# --- three-component version ---"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# --- three-component version ---'': Failed to match sequence (PREFIX SLUG)
    at line 1 char 1.'
- id: oasis.debt.2375355675537127803
  input: "# --- spec + version + part (Pt spelling) ---"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# --- spec + version + part (Pt spelling) ---'': Failed to match sequence (PREFIX
    SLUG) at line 1 char 1.'
- id: oasis.debt.1119234662624931219
  input: "# --- spec + version + stage + part ---"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# --- spec + version + stage + part ---'': Failed to match sequence (PREFIX
    SLUG) at line 1 char 1.'
- id: oasis.debt.2909385839978429616
  input: "# --- part-before-stage order variant ---"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# --- part-before-stage order variant ---'': Failed to match sequence (PREFIX
    SLUG) at line 1 char 1.'
- id: oasis.debt.1154880259339678715
  input: "# --- label between fragments ---"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# --- label between fragments ---'': Failed to match sequence (PREFIX SLUG)
    at line 1 char 1.'
- id: oasis.debt.3623570284402402925
  input: "# --- trailing free-text label ---"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# --- trailing free-text label ---'': Failed to match sequence (PREFIX SLUG)
    at line 1 char 1.'
- id: oasis.debt.1549583483343075797
  input: "# --- stage tokens: CS0x / PS0x ---"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# --- stage tokens: CS0x / PS0x ---'': Failed to match sequence (PREFIX SLUG)
    at line 1 char 1.'
- id: oasis.debt.2424439142621146861
  input: "# --- errata ---"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# --- errata ---'': Failed to match sequence (PREFIX SLUG) at line 1 char 1.'
- id: oasis.debt.3673350278320943174
  input: "# --- mixed-case spec names ---"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# --- mixed-case spec names ---'': Failed to match sequence (PREFIX SLUG) at
    line 1 char 1.'
- id: oasis.debt.3423353953258398859
  input: "# --- malformed records (must still round-trip via verbatim original) ---"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse OASIS identifier
    ''# --- malformed records (must still round-trip via verbatim original) ---'':
    Failed to match sequence (PREFIX SLUG) at line 1 char 1.'
