---
- id: tgpp.debt.4445120349185889295
  input: "# 3GPP Technical Report - representative sample"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse 3GPP identifier
    ''# 3GPP Technical Report - representative sample'': Failed to match sequence
    (PUBLISHER_PREFIX? TYPE SPACE NUMBER_CORE SUFFIX? PARTS ('':'' RELEASE)? ''/''
    VERSION) at line '
- id: tgpp.debt.2601097260169325476
  input: "# Sampled from relaton-data-3gpp (index-v1.yaml). Covers every release form"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse 3GPP identifier
    ''# Sampled from relaton-data-3gpp (index-v1.yaml). Covers every release form'':
    Failed to match sequence (PUBLISHER_PREFIX? TYPE SPACE NUMBER_CORE SUFFIX? PARTS
    ('':'' REL'
- id: tgpp.debt.1074365985163019486
  input: "# (REL-, Ph, UMTS), the 'U' letter suffix and part depths."
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse 3GPP identifier
    ''# (REL-, Ph, UMTS), the ''U'' letter suffix and part depths.'': Failed to match
    sequence (PUBLISHER_PREFIX? TYPE SPACE NUMBER_CORE SUFFIX? PARTS ('':'' RELEASE)?
    ''/'' VERSIO'
- id: tgpp.debt.4332500545698485560
  input: "# 3GPP Technical Specification - representative sample"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse 3GPP identifier
    ''# 3GPP Technical Specification - representative sample'': Failed to match sequence
    (PUBLISHER_PREFIX? TYPE SPACE NUMBER_CORE SUFFIX? PARTS ('':'' RELEASE)? ''/''
    VERSION) a'
- id: tgpp.debt.2601097260169325476
  input: "# Sampled from relaton-data-3gpp (index-v1.yaml). Covers every release form"
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse 3GPP identifier
    ''# Sampled from relaton-data-3gpp (index-v1.yaml). Covers every release form'':
    Failed to match sequence (PUBLISHER_PREFIX? TYPE SPACE NUMBER_CORE SUFFIX? PARTS
    ('':'' REL'
- id: tgpp.debt.1207984100955435643
  input: "# (REL-, Ph, Release N), letter suffixes (dcs, ext) and part depths."
  expect:
    error:
      code: unclassified
  notes: 'unparsed ground-truth fixture: RuntimeError: Failed to parse 3GPP identifier
    ''# (REL-, Ph, Release N), letter suffixes (dcs, ext) and part depths.'': Failed
    to match sequence (PUBLISHER_PREFIX? TYPE SPACE NUMBER_CORE SUFFIX? PARTS ('':''
    RELEASE)? '
