# frozen_string_literal: true

# Dev-only: RSpec for the tool-guard spec suite. The validators and the
# provenance generator stay gem-free - CI runs them with plain `ruby`
# before `bundle install`, and that must keep working.
source "https://rubygems.org"
gem "rspec", "~> 3.13"
